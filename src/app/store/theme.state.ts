// src/app/store/theme.state.ts

// Define the ThemeState interface
export interface ThemeState {
  themeName: string; // Name of the current theme
  isDarkMode: boolean; // Flag indicating if dark mode is enabled
}
